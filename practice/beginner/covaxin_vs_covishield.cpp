#include <bits/stdc++.h>

#define ll unsigned long long int;

int main() {
  int t = 0;
  scanf("%d", &t);
  while (t--) {
    ll x = 0;
    std::vector<ll> a(1), b(1), c(1), d(1);
    scanf("%llu %llu %llu %llu %llu", &x, &a[0], &b[0], &c[0], &d[0]);
  }
}
Chef lives in an N×M grid. e is currently participating in a treasure hunt, 
    nd has two items left to find. hef kno
